extends layout

block content
  h1
    p  Do you really want to delete this Instance?

  h3 #{title}: id: #{instance._id} || status: #{instance.status}

      form(method='POST' action='')
        div.form-group
        input#authorid.form-control(type='hidden',name='instanceid', required='true', value=instance._id )

        button.btn.btn-primary(type='submit') Delete
